[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py
testpaths = tests
addopts = --tb=short -m "not browser"
markers =
    browser: end-to-end tests that need a real Chromium (run with -m browser)
